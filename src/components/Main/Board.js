import React from "react";
import styled from "styled-components";

const BoardBox = styled.div`
  padding: 2rem;
`;

const Board = () => {
  return <BoardBox>로그인 완료됬습니다.</BoardBox>;
};

export default Board;
